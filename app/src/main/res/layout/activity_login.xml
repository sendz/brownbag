<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="viewModel"
            type="sendz.github.io.brownbag.login.LoginViewModel"/>
        <variable
            name="presenter"
            type="sendz.github.io.brownbag.login.LoginPresenter"/>
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/activity_login"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="sendz.github.io.brownbag.login.LoginActivity">

        <android.support.v7.widget.AppCompatEditText
            android:text="@={viewModel.username}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/string_username" />

        <android.support.v7.widget.AppCompatEditText
            android:text="@={viewModel.password}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/string_password"
            android:inputType="textPassword" />

        <android.support.v7.widget.AppCompatButton
            android:onClick="@{v->presenter.doLogin()}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:enabled="@{viewModel.loginButtonEnable}"
            android:text="@string/string_login" />

        <TextView
            android:text="@{viewModel.message}"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="@{viewModel.visibility}"
            tools:text="Your username is" />
    </LinearLayout>
</layout>